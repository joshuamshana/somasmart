# JWT signing secret for tenant and platform token classes
JWT_SECRET=replace-with-strong-random-secret

# Postgres connection (Prisma schema target)
DATABASE_URL=postgresql://joshua:joshua@localhost/blackdb

# Server bind
PORT=4000
HOST=0.0.0.0
